name: deliver deploy

on:
  workflow_dispatch:
    inputs:
      beanstalk-env:
        description: Beanstalk Env
        required: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Test input
        env:
          BEANSTALK: ${{ github.event.inputs.logLevel }} 
        run: |
          echo "testing input: ${{ github.event.inputs.beanstalk-env }}"
          echo "print env $INPUT_BEANSTALK-ENV"
          echo "print preset env $BEANSTALK"
          echo $GITHUB_REF
